pipeline {
    agent any

    stages {
        stage('Testing maven') {
            steps {
                sh "mvn -version"
            }
        }
        stage('Testing maven') {
            steps {
                sh "git --version"
            }
        }
    }
}
